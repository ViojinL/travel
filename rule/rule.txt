不要采用 fetch、axios 等异步方式完成，应采取 form 提交、
Servlet 处理、显示结果页面形式。（5）保持代码完整性，如某一段代码使用了额外的、自
定义方法或类，应在该处加上这些方法或类。
有如下旅游景点管理系统：用户登录后，有不同的城市，可以对城市进行管理。每个城市
有自己有特色的旅游景点，可以管理旅游景点，也可以为景点上传图片。用户、城市、景
点信息都存放在数据库中。
1 显示所有城市：index.jsp 中有该 Servlet 链接。Servlet 得到所有城市，在页面用表格显示
所有城市的信息，城市的信息至少要有编号（自增）、名字、所属省，每个城市后有对应
的操作超链接：编辑、显示该城市的所有景点。页面有新建超链接，用于新建城市。该页
面可以显示新建、编辑等操作的结果。（1.1 得到所有城市 Servlet，1.2 显示所有城市页面）
2 新建城市：显示新建城市的输入页面，用户提交后，Servlet 中接收数据并保存在数据库
中，准备操作结果信息，转到显示所有城市。（2.1 城市输入页面，2.2 保存城市 Servlet）
3 编辑城市：Servlet 得到指定 id 的城市，在编辑页面显示原有城市的信息进行编辑。提交
后在 Servlet 中更新，准备操作结果信息，转到显示所有城市。（3.1 得到城市 Servlet，3.2
城市编辑页面，3.3 更新城市 Servlet）
4 显示城市下的所有景点：得到指定城市下的所有景点，用表格显示景点的信息和操作，
景点信息至少有 id、名字、门票价格、简介、图片，每个景点只有一张图片。该页面不用
显示图片。每个景点对应的操作有上传图片超链接。该页面有增加景点超链接。（4.1 得到
城市下所有景点 Servlet，4.2 显示所有景点页面）
5 新建景点：显示新建景点页面，输入名字、门票价格、简介，用户提交后，在 Servlet 中
保存到数据库，转到显示该城市所有景点。（5.1 景点输入页面，5.2 保存景点 Servlet）
6 上传景点图片：上传页面，在 Servlet 中保存选定的上传图片，不考虑文件重名问题，和
景点的关系保存在数据库中。完成后转到显示该景点详细信息页面，显示 id、名字、门票
价格、简介、图片，显示详细信息页面有显示该城市所有景点的链接。（6.1 上传页面，6.2
保存上传图片 Servlet，6.3 得到该景点详细信息 Servlet，6.4 显示该景点详细信息页面）
7 数据库通用操作。（7.1 更新操作，7.2 查询操作）

2.2 禁用技术

❌ 不使用 Ajax/Fetch/Axios 等异步请求
❌ 不使用前端框架(Vue/React/Angular)
❌ 不使用 Bootstrap 等 UI 框架
❌ 不使用 jQuery 或其他 JavaScript 库
 命名规范

表名: 小写单词,多个单词用下划线分隔
字段名: 小写单词,多个单词用下划线分隔
主键统一命名为 id
外键命名格式: 关联表名_id

6.2 禁止使用的样式

❌ 复杂的渐变效果
❌ 动画和过渡效果
❌ 阴影和圆角(除非极简设计需要)
❌ 自定义字体
❌ 响应式媒体查询(非必需)
SQL 注入防护: 使用 PreparedStatement
Session 管理: 合理使用 Session 存储用户状态
参数验证: 前后端都要验证参数

11.1 代码完整性检查

 每个 Servlet 是否包含完整的依赖类?
 是否提供了所有自定义工具类的完整代码?
 DAO 方法是否包含完整的 CRUD 实现?
 是否提供了 DBUtil 等通用工具类?

11.2 功能检查

 表单是否使用 POST 提交?
 Servlet 是否正确处理请求参数?
 是否使用 forward 或 redirect 跳转?
 是否正确显示操作结果消息?
 文件上传是否保存成功?

11.3 样式检查

 页面背景是否为白色?
 是否避免使用复杂 CSS?
 表格是否清晰显示数据?
 链接和按钮是否易于识别?

11.4 规范检查

 命名是否符合规范?
 URL 映射是否统一?
 是否使用 JSTL 而非脚本?
 资源是否正确关闭?

十二、禁止事项

禁止使用异步请求: 不使用 Ajax/Fetch/Axios
禁止前端框架: 不使用 Vue/React/Angular
禁止过度样式: CSS 保持最小化,不使用复杂特效
禁止不完整代码: 必须提供所有依赖的类和方法
禁止硬编码: 路径使用 ${pageContext.request.contextPath}
禁止 JSP 脚本: 优先使用 JSTL 标签